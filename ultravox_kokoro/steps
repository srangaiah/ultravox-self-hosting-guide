sudo apt-get update
sudo apt-get upgrade

clone repository - logion

install python extension

pip install -r requirements.txt
cfreate venv virtual environment

# For Ubuntu/Debian:
sudo apt-get install libportaudio2

sudo apt-get install espeak-ng portaudio19-dev python3-pyaudio

